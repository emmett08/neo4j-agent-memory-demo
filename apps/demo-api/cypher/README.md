# Demo API Cypher (Knowledge Graph)

These scripts are designed for Neo4j Browser / Neo4j VSCode to explore the seeded knowledge graph created by `npm run db:seed`.

Seeded agent ids (PascalCase): `Seed`, `Auggie`, `Agent1`, `Agent2`, `Agent3`.

